global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "evalforge-backend"
    metrics_path: "/api/v1/metrics"
    scrape_interval: 10s
    static_configs:
      - targets: ["backend:8000"]
